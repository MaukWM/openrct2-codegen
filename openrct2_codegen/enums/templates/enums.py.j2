# Auto-generated by openrct2-codegen {{ generator_version }}
# OpenRCT2 {{ openrct2_version }}
# Generated: {{ generated_at }}
#
# DO NOT EDIT — regenerate with:
#   openrct2-codegen render --template enums.py --out <path>

from enum import IntEnum, IntFlag

# OpenRCT2 version these enums were generated against.
GENERATED_OPENRCT2_VERSION: str = "{{ openrct2_version }}"

{% for enum_name, enum_def in enums.items() %}

class {{ enum_name }}({{ "IntFlag" if enum_def["kind"] == "flags" else "IntEnum" }}):
{% for val in enum_def["values"] %}
    {{ val.name | to_screaming_snake }} = {{ val.value }}
{% endfor %}
{% endfor %}

# ── Sentinel constants ───────────────────────────────────────────────
# Not enum members — these are sentinel values that live outside the enum range.

# kInvalidDirection (0xFF) from Location.hpp. Means "no direction" or "auto".
# Used by footpathplace for auto-connect mode. Typed as int so it doesn't
# pollute Direction iteration.
INVALID_DIRECTION: int = 255
